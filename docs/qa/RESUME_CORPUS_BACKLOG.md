# Resume Corpus Backlog

- Add more synthetic DOCX variants with tables, headers/footers, and icon-like bullets.
- Add a deliberately malformed DOCX generated at runtime.
- Add password-protected PDF coverage if a local generator/tool is approved.
- Add image-only PDF fixture generated from synthetic text rendered as an image.
- Add known-safe sanitized metadata for selected private real resumes so quality checks can assert preservation without exposing PII.
- Add export validation for DOCX if/when DOCX export exists.
- Add visual PDF comparison once `LATEX_COMPILE_URL` is available in a safe QA environment.
